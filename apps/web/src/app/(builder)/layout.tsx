/**
 * Builder route group layout — full-screen, no AppShell sidebar/topbar.
 * The builder page renders its own top bar and step lane.
 */
export default function BuilderLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
